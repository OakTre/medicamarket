mixin button(name, icon, mod)
  button.button(class=mod)&attributes(attributes)
    span.button__inner
      if icon
        +icon-mono(`${icon}`)(class="button__icon" width="24" height="24")
      span.button__name
        | #{name}
